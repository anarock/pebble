import { css } from "emotion";

export const wrapper = css({
  position: "relative"
});

export const optionsWrapper = css({
  width: "100%",
  position: "absolute",
  marginTop: -40,
  zIndex: 999
});
